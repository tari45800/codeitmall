import React from "react";
import styles from "@/styles/Home.module.css";

const Setting = () => {
  return (
    <div>
      <div className={styles.title}>Setting입니다.</div>
    </div>
  );
};

export default Setting;
